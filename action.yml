name: 'issue-autotagger'
description: 'Tag issues extracted with contents of their description'
inputs:
  repo-token:  # id of input
    description: 'Github Token'
    required: true
runs:
  using: 'node12'
  main: 'dist/index.js'